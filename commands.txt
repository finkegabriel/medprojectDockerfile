docker build -t es_sshd .
docker run -d -P --name test_sshd es_sshd
docker port test_sshd 22


in docker container

npm cache clean -f
npm install -g n
n stable
mkdir cookies

chromium-browser

 docker image rm *
 docker image ls
 docker container ls -a